# ElohimOS Configuration
# Copy this file to .env and customize as needed

# ===== Platform Requirements =====
# ElohimOS is macOS-only and offline-first.
# - OS: macOS (Darwin) only
# - AI: Local Ollama (localhost:11434) - no cloud calls
# - Prerequisites: brew install ffmpeg python@3.11; install Ollama

# ===== Data Directories =====

# Base data directory for all ElohimOS data
# Default: .neutron_data
# ELOHIMOS_DATA_DIR=.neutron_data

# Temporary uploads directory
# Default: temp_uploads
# ELOHIMOS_TEMP_DIR=temp_uploads

# Temporary exports directory
# Default: temp_exports
# ELOHIMOS_EXPORTS_DIR=temp_exports

# ===== API Configuration =====

# API server host
# Default: 0.0.0.0
# ELOHIMOS_HOST=0.0.0.0

# API server port
# Default: 8000
# ELOHIMOS_PORT=8000

# ===== Rate Limiting =====

# Default rate limit for all endpoints (requests per minute)
# Default: 100/minute
# ELOHIMOS_RATE_LIMIT_DEFAULT=100/minute

# Rate limit for file uploads (requests per minute)
# Default: 10/minute
# ELOHIMOS_RATE_LIMIT_UPLOADS=10/minute

# Rate limit for SQL queries (requests per minute)
# Default: 60/minute
# ELOHIMOS_RATE_LIMIT_QUERIES=60/minute

# Rate limit for admin operations (requests per hour)
# Default: 3/hour
# ELOHIMOS_RATE_LIMIT_ADMIN=3/hour

# ===== Database Configuration =====

# SQLite database connection pool size
# Default: 5
# ELOHIMOS_DB_POOL_SIZE=5

# ===== Logging =====

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Default: WARNING
# ELOHIMOS_LOG_LEVEL=WARNING

# ===== Security =====

# Enable Secure Enclave for key storage (macOS only)
# Default: true
# ELOHIMOS_USE_SECURE_ENCLAVE=true

# CORS allowed origins (comma-separated)
# Default: http://localhost:4200,http://localhost:5173,http://localhost:3000
# ELOHIMOS_CORS_ORIGINS=http://localhost:4200,http://localhost:5173

# ===== P2P Networking =====

# P2P discovery port
# Default: 5353
# ELOHIMOS_P2P_PORT=5353

# P2P service name
# Default: _elohimos._tcp.local.
# ELOHIMOS_P2P_SERVICE=_elohimos._tcp.local.
