# ElohimOS Environment Configuration Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# Development mode - enables dev defaults and relaxed rate limiting
ELOHIM_ENV=development

# Founder Rights Account - Superuser credentials
# ⚠️  SECURITY: Change these immediately on first startup!
ELOHIM_FOUNDER_USERNAME=founder
ELOHIM_FOUNDER_PASSWORD=CHANGE_ME_IMMEDIATELY

# JWT Secret - Used to sign authentication tokens
# Generate with: openssl rand -hex 32
# ⚠️  SECURITY: Must be unique per environment, never reuse!
ELOHIMOS_JWT_SECRET_KEY=GENERATE_ME_WITH_OPENSSL_RAND_HEX_32

# Optional: API Configuration
# ELOHIMOS_API_PORT=8000
# ELOHIMOS_CORS_ORIGINS=["http://localhost:4200","http://127.0.0.1:4200"]

# Optional: Database Encryption
# Generate with: openssl rand -hex 32
# ELOHIMOS_DB_ENCRYPTION_KEY=GENERATE_ME_WITH_OPENSSL_RAND_HEX_32
