from fastapi import APIRouter

# Placeholder router for file upload / SQL / JSON endpoints under /api/sessions/{id}/*
# No routes defined yet to ensure API parity prior to code migration.
router = APIRouter()

