//
//  HubConstants.swift
//  MedStation
//
//  Shared layout constants for the Workspace Hub.
//

import SwiftUI

enum HubLayout {
    /// Standard header bar height across all Hub panes.
    /// Applied to sidebar header, list headers, toolbars, and content headers
    /// so that the Divider() below each forms one continuous horizontal line.
    static let headerHeight: CGFloat = 44
}
