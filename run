#!/bin/bash

# OmniStudio - Simple run command
# Just type './run' or 'omni' to start everything!

# Change to the directory where this script is located
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$SCRIPT_DIR"

echo "🌟 OmniStudio"
echo "━━━━━━━━━━━━━━━"
echo ""

# Always run web version
./start_web.sh
